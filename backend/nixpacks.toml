[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = [
    "npm run build",
    "ls -la dist/ || echo 'dist folder not found'"
]

[start]
cmd = "node dist/main.js"
