[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate",
  "rm -rf dist",
  "npx tsc -p tsconfig.build.json --listEmittedFiles",
  "ls -la dist/"
]

[start]
cmd = "npx prisma migrate deploy && node dist/main.js"
