{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm install tsc-alias && npx prisma generate && rm -rf dist && npx tsc --project tsconfig.json --outDir dist --declaration false && npx tsc-alias -p tsconfig.json --outDir dist"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npx prisma db seed && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
