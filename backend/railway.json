{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && rm -rf dist && npx tsc --project tsconfig.json --outDir dist --declaration false"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node -r tsconfig-paths/register dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
