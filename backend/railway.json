{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && rm -rf dist && npx tsc -p tsconfig.build.json && ls -la dist/"
  },
  "deploy": {
    "startCommand": "echo DATABASE_URL length: ${#DATABASE_URL} && npx prisma migrate deploy && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
